# agents-radar configuration
# Only generate OpenClaw ecosystem daily report

cli_repos: []

skills_repo: ""

openclaw:
  id: openclaw
  repo: openclaw/openclaw
  name: OpenClaw
  paginated: true

openclaw_peers: []
